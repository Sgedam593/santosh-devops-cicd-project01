pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Welcome Santosh'
            }
        }

        stage('Docker Version') {
            steps {
                sh 'docker --version'
            }
        }

        stage('Current Directory') {
            steps {
                sh 'pwd'
            }
        }

    }

}
